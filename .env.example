# No environment variables required.
#
# Trading 212 credentials are now encrypted client-side under each user's
# master secret (see src/lib/crypto-client.ts) — the server never sees a
# stable key. The previous T212_ENCRYPTION_KEY env var is obsolete.
