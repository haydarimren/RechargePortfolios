# Master key used to encrypt broker API credentials at rest in Firestore.
# Generate with: openssl rand -base64 32
# MUST be set in every environment (local, Vercel Preview, Vercel Production).
# If rotated without re-encrypting stored blobs, existing users must reconnect.
T212_ENCRYPTION_KEY=
